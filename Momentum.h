#pragma once

#include <list>
#include <vector>

namespace neural_network {
// class Momentum {
// public:
//     Momentum(double step, double momentum_step);
//     void update(const std::vector<Matrix>& grads,
//                 std::vector<Linear>* linear_layers);
//     std::string describe() const;

// private:
//     std::vector<Vector> zerosInversed(std::vector<Linear>* parameters);

//     std::list<Linear>& linear_layers_;
//     double step_;
//     double m_;
//     std::vector<Vector> h_;
// };
}  // namespace neural_network
